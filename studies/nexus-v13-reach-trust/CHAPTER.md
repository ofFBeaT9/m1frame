# Chapter: NEXUS v1.3 "Reach & Trust"
date: 2026-06-11
topic: Cycle 3 — patient portal, communication hub, print/export, imaging scaffold,
and REAL trust-substrate steps (durable persistence, WORM hash-chain audit, e-signatures).

## Phase 0 — Recall (in-repo miras records, cycles 1-2)
- Queue (cycle-2 FINAL_REPORT, council-ranked): portal §14 first (most underserved persona),
  print §12 second (completes the legal loop), substrate third, chat before video, imaging last.
- Constraints in force: demo-grade single dev; no architectural rewrite of the store choke-point;
  HONEST scope labels (offline AI precedent: degrade, never fake); spotless = Residual Retirement.
- Lessons in force: tests arbitrate (council prescriptions failed twice in cycle 2); diff rule
  tables against cited sources; 404-cloak mutation-by-id routes; lazy sweeps for timed duties;
  $-in-route Express quirk; brief parallel review lenses on in-flight dev work.
- Honesty boundaries fixed up front for this cycle: video = call-session management + waiting
  room (no media transport); print = print-optimized views via browser print (no server PDF);
  imaging = study records + structured reports (no DICOM rendering); e-signature = HMAC under
  server key custody (integrity + attribution, NOT legal non-repudiation); persistence = JSON
  snapshot with a bounded crash window (not a transactional DB). Each labelled in README.
